import * as React from 'react';
import './Logo.css';

// Probably does not need to be it's own component as so small
// Could be useful if needed to do more with the logo
function Logo() {
    return <div className='Logo'>Y</div>;
};


export default Logo;